## Habit Arena Server

Express backend for Habit Arena.

```bash
npm install
cp .env.example .env
npm run dev
```

Local API runs at `http://localhost:5050`.

- `GET /`
- `GET /health`

Run `../supabase/schema.sql` in Supabase and add backend Supabase env values before testing Week 2 challenge routes.
